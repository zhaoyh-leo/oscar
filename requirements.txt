# OSCAR runtime dependencies (Python >= 3.11)
langgraph>=1.0
langchain-core>=1.0
langchain-openai>=1.0
langchain-anthropic>=0.3
pydantic>=2.0.0
httpx>=0.27.0
PyMuPDF>=1.25.0
rich>=13.7

# Code/paper vector stores: CodeBERT embeddings + FAISS + BM25
numpy>=1.26
torch>=2.2
transformers>=4.40
faiss-cpu>=1.8
rank-bm25>=0.2.2

# git cloning is done via the system `git` binary (subprocess); GitPython is unused.
# Kept only for parity with pyproject.toml — safe to remove.
gitpython>=3.1.0

# Layered configuration: .env secrets + config.yaml tuning
python-dotenv>=1.0
pyyaml>=6.0

# Model downloader (scripts/download_models.py) — only needed to fetch bert/
huggingface-hub>=0.23
